import 'package:flutter/material.dart';

class SMSParserPage extends StatelessWidget {
  final route = MaterialPageRoute(builder: (context) => SMSParserPage());

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("SMS Parser"),
    );
  }
}
